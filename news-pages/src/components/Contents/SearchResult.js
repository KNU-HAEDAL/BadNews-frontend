
const SearchResult = () => {
    // let match = useMatch(); /* 현재 경로의 path */
    
    return (
        <div className="SearchResult">
        
        </div>
    );
};

export default SearchResult;
